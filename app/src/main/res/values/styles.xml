<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="VmorTextStyle">
        <item name="android:padding">20dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="EditTextDarkStyle" parent="VmorTextStyle">
        <item name="android:background">@color/backgroundDark</item>
    </style>

    <style name="EditTextLightStyle" parent="VmorTextStyle">
        <item name="android:background">@color/backgroundLight</item>
    </style>

    <style name="ResultTextViewStyle" parent="VmorTextStyle">
        <item name="android:textSize">25sp</item>
        <item name="android:background">@color/backgroundResult</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
